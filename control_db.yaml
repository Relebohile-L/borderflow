apiVersion: v1
kind: Service
metadata:
  name: control-db
spec:
  clusterIP: None
  selector:
    app: control-db
  ports:
    - port: 3306
---
apiVersion: v1
kind: Service
metadata:
  name: control-db-service
spec:
  type: NodePort
  selector:
    app: control-db
  ports:
    - port: 3306
      targetPort: 3306
      nodePort: 30010
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: control-db
spec:
  serviceName: control-db
  replicas: 1
  selector:
    matchLabels:
      app: control-db
  template:
    metadata:
      labels:
        app: control-db
    spec:
      containers:
        - name: mariadb
          image: mariadb:10.6
          env:
            - name: MYSQL_ROOT_PASSWORD
              value: root
            - name: MYSQL_DATABASE
              value: borderflow_control
          ports:
            - containerPort: 3306
          volumeMounts:
            - name: data
              mountPath: /var/lib/mysql
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 1Gi