apiVersion: v1
kind: Service
metadata:
  name: sa-db
spec:
  clusterIP: None
  selector:
    app: sa-db
  ports:
    - port: 3306
---
apiVersion: v1
kind: Service
metadata:
  name: sa-db-service
spec:
  type: NodePort
  selector:
    app: sa-db
  ports:
    - port: 3306
      targetPort: 3306
      nodePort: 30013
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: sa-db
spec:
  serviceName: sa-db
  replicas: 1
  selector:
    matchLabels:
      app: sa-db
  template:
    metadata:
      labels:
        app: sa-db
    spec:
      containers:
        - name: mariadb
          image: mariadb:10.6
          env:
            - name: MYSQL_ROOT_PASSWORD
              value: root
            - name: MYSQL_DATABASE
              value: borderflow_sa
          ports:
            - containerPort: 3306
          volumeMounts:
            - name: data
              mountPath: /var/lib/mysql
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 1Gi