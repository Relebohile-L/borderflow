apiVersion: v1
kind: Service
metadata:
  name: mru-db
spec:
  clusterIP: None
  selector:
    app: mru-db
  ports:
    - port: 3306
---
apiVersion: v1
kind: Service
metadata:
  name: mru-db-service
spec:
  type: NodePort
  selector:
    app: mru-db
  ports:
    - port: 3306
      targetPort: 3306
      nodePort: 30011
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mru-db
spec:
  serviceName: mru-db
  replicas: 1
  selector:
    matchLabels:
      app: mru-db
  template:
    metadata:
      labels:
        app: mru-db
    spec:
      containers:
        - name: mariadb
          image: mariadb:10.11
          env:
            - name: MYSQL_ROOT_PASSWORD
              value: root
            - name: MYSQL_DATABASE
              value: borderflow_mru
          ports:
            - containerPort: 3306
          volumeMounts:
            - name: data
              mountPath: /var/lib/mysql
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 1Gi